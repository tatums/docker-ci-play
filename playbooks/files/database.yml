defaults: &defaults
  adapter: postgresql
  encoding: unicode
  username: postgres
  password:
  host: db
  pool: 5

development:
  <<: *defaults
  database: tatum_im_development

production:
  <<: *defaults
  database: tatum_im_production

test:
  <<: *defaults
  database: tatum_im_test
